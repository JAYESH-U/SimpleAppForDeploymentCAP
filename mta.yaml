_schema-version: 3.3.0
ID: simpleProjectForDeployment
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: simpleProjectForDeployment-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: simpleProjectForDeployment-auth
      - name: simpleProjectForDeployment-destination
      - name: simpleProjectForDeployment-db

  - name: simpleProjectForDeployment-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: simpleProjectForDeployment-auth
      - name: srv-api
      - name: simpleProjectForDeployment-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: resources/
      requires:
        - name: simpleProjectForDeploymentui_customers
          artifacts:
            - ui_customers.zip
          target-path: resources/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
  - name: simpleProjectForDeployment-destinations
    type: com.sap.application.content
    requires:
      - name: simpleProjectForDeployment-auth
        parameters:
          service-key:
            name: simpleProjectForDeployment-auth-key
      - name: simpleProjectForDeployment-html5-repo-host
        parameters:
          service-key:
            name: simpleProjectForDeployment-html5-repo-host-key
      - name: srv-api
      - name: simpleProjectForDeployment-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: simpleProjectForDeployment-html5-repository
              ServiceInstanceName: simpleProjectForDeployment-html5-repo-host
              ServiceKeyName: simpleProjectForDeployment-html5-repo-host-key
              sap.cloud.service: simpleProjectForDeployment.service
            - Name: simpleProjectForDeployment-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: simpleProjectForDeployment-auth
              ServiceKeyName: simpleProjectForDeployment-auth-key
              sap.cloud.service: simpleProjectForDeployment.service

  - name: simpleProjectForDeploymentui_customers
    type: html5
    path: app/ui_customers
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: simpleProjectForDeployment-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: simpleProjectForDeployment-db
resources:
  - name: simpleProjectForDeployment-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: simpleProjectForDeployment-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'CustomerViewer (simpleProjectForDeployment ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.CustomerViewer'
          - name: 'CustomerManager (simpleProjectForDeployment ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.CustomerManager'
  - name: simpleProjectForDeployment-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: simpleProjectForDeployment-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: simpleProjectForDeployment-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: simpleProjectForDeployment-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
